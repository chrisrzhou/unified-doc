---
name: Transform
menu: Gallery
route: gallery/transform
---

import Transform from './src/transform';

# Transform

> TODO: An example showing how to transform annotated `mark` nodes into other nodes.
>
> This is accomplished by replacing removing all `mark` nodes except the ones with `data-start="true"` tag attribute and replacing these nodes with another DOM node.
