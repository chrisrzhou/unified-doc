import { createProcessor } from '.';

describe('processor.js', () => {
	describe('createProcessor', () => {
		it('should create default processor and parse content correctly', () => {});

		it('should process annotations for html content', () => {});

		it('should process annotations for markdown content', () => {});

		it('should process annotations for text content', () => {});

		it('should apply text extractor', () => {});

		it('should apply annotation callbacks', () => {});
	});
});
